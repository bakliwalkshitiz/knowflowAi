spring:
  datasource:
    url: jdbc:mysql://localhost:3306/knowflow_ai
    username: root
    password: Bakliwal@mysql

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true